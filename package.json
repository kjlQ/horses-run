{
  "name": "run",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "serve": "^14.0.1"
  },
  "scripts": {
    "dev": "react-scripts start",
    "start": "serve -s build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "heroku-postbuild": "cd server & npm start --parallel & cd client & npm start"
  }
}
